# Server
server.port=8080

# Database (Neon PostgreSQL)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-ancient-haze-aheqs81z-pooler.c-3.us-east-1.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_Eq32RlQZxhDf}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT
# Minimum 256 bits (32 characters) required for HMAC-SHA algorithms
# For production, set JWT_SECRET environment variable with a secure 32+ character string
jwt.secret=${JWT_SECRET:MySecretKeyForJWTTokenGenerationAndValidationInRentEaseApplication2024SecureKey}
jwt.expiration=86400000
